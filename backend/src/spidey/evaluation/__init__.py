"""Evaluation bounded context: suites, runners, baselines, reports.

Code lives here; datasets, blessed baselines, and generated reports live in
the top-level ``evaluation/`` directory (data, not code). See docs/10.
"""
